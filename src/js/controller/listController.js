// Here we get the module we created in file app.js
var toastr = require("toastr");
export default function listController($scope, $http, $base64, $routeParams, $window) {
    $scope.text = "Weiter zu details";
}
